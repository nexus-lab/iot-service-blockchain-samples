# Depends on device_registry:Register
test:
  name: device_register:Deregister

  workers:
    type: remote
    number: 1

  rounds:
    - label: default
      txNumber: 2
      rateControl:
        type: fixed-rate
        opts:
          tps: 50
      workload:
        module: workloads/device-registry/deregister.js
