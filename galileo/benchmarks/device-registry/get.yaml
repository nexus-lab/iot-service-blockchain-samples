# Depends on device_registry:Register
test:
  name: device_register:Get

  workers:
    type: remote
    number: 5

  rounds:
    - label: default
      txNumber: 5000
      rateControl:
        type: fixed-rate
        opts:
          tps: 50
      workload:
        module: workloads/device-registry/get.js
