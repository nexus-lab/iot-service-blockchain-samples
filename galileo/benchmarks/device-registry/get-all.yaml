# Depends on device_registry:Register
test:
  name: device_register:GetAll

  workers:
    type: remote
    number: 1

  rounds:
    - label: default
      txNumber: 2
      rateControl:
        type: fixed-rate
        opts:
          tps: 50
      workload:
        module: workloads/device-registry/get-all.js
