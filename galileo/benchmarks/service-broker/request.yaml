# Depends on service_registry:Register
test:
  name: service_broker:Request

  workers:
    type: remote
    number: 1

  rounds:
    - label: default
      txNumber: 2
      rateControl:
        type: fixed-rate
        opts:
          tps: 50
      workload:
        module: workloads/service-broker/request.js
