# Depends on service_broker:Request
test:
  name: service_broker:Remove

  workers:
    type: remote
    number: 5

  rounds:
    - label: default
      txNumber: 5000
      rateControl:
        type: fixed-rate
        opts:
          tps: 50
      workload:
        module: workloads/service-broker/remove.js
